{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/records", "dest": "/api/records" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
